/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/nativephp
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
NUL
storage/.DS_Store
.DS_Store

# Credential files (keystores, private keys, etc.)
/credentials/

# Sibling package checkouts used as Composer path repositories. Each is its own
# git repo; they are cloned next to this app, not carried inside it.
/packages/

# Backups left by switching the app between package branches
*.bak
